# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
build/
dist/
wheels/
*.egg-info/
.eggs/
.Python

# ── Virtual environments ──
.venv/
venv/
env/
ENV/

# ── Editors / IDE ──
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Claude Code / AI tooling ──
.claude/
.cursor/

# ── Secrets & local config ──
.env
*.env.local
.env.*.local

# ── Models (users fetch via README instructions) ──
*.onnx
*.task
*.bin
*.pt
*.gguf
*.safetensors
models/

# ── Runtime artifacts ──
logs/
*.log
tts_cache/
conversation_log.jsonl
*.jsonl
!example_*.jsonl

# ── Temporary ──
tmp/
*.tmp
*.pid
*.sock

# ── User-specific dev notes ──
HANDOVER.md
SCRATCH.md
archive/

# ── Session scratch (file exchange with remote deploy host, NOT source) ──
_s1_edit/
_publish/
_publish.tar.gz
_pr_body.md
bash.exe.stackdump

# ── Separate public bench repo artefacts (see qwen3.6-specdec-rtx3090 repo) ──
bench/
bench_llm.py
bench_qwen36.py
bench_tts.py

# ── Runtime state written by robot-brain on the deploy host ──
conversation_summary.txt
conversation_summary.txt.watermark
.qdrant_memory/

# ── pytest + lint caches ──
.pytest_cache/
.ruff_cache/
